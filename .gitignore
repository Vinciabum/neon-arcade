node_modules
.DS_Store
Thumbs.db
*.log
.gemini

# build output — generated by build.js, never committed
/index.html
/games/
/about/
/contact/
/privacy/
/sitemap.xml
/robots.txt
/404.html
/llms.txt
/ads.txt

# design/plan docs — local only until you decide to publish (public repo)
/docs/superpowers/

# agent + skill definitions — local only. 공개 리포라 자동 생성 파이프라인이
# 그대로 보이면 포털 심사에서 불리하게 읽힐 수 있다.
/.claude/

# 포털 납품 빌드 산출물
/dist/
